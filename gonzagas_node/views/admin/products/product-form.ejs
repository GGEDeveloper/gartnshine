<div class="container-fluid">
    <!-- Page Heading -->
    <div class="d-sm-flex align-items-center justify-content-between mb-4">
        <h1 class="h3 mb-0 text-gray-800"><%= title %></h1>
        <% if (product && product.id) { %>
            <a href="/admin/products/<%= product.id %>" class="btn btn-outline-primary btn-sm">
                <i class="fas fa-eye"></i> Visualizar
            </a>
        <% } %>
    </div>

    <%- include('../partials/flash-messages') %>

    <div class="card shadow mb-4">
        <div class="card-header py-3">
            <h6 class="m-0 font-weight-bold text-primary">
                <%= product && product.id ? 'Editar' : 'Novo' %> Produto
            </h6>
        </div>
        <div class="card-body">
            <form 
                action="<%= product && product.id ? `/admin/products/${product.id}` : '/admin/products' %>" 
                method="POST" 
                enctype="multipart/form-data"
                class="needs-validation"
                novalidate
                data-notification="Salvando produto..."
                data-notification-type="info"
            >
                <% if (product && product.id) { %>
                    <input type="hidden" name="_method" value="PUT">
                <% } %>

                <div class="row">
                    <!-- Coluna Esquerda -->
                    <div class="col-md-6">
                        <!-- Informações Básicas -->
                        <div class="form-group mb-3">
                            <label for="name" class="form-label">Nome do Produto *</label>
                            <input 
                                type="text" 
                                class="form-control" 
                                id="name" 
                                name="name" 
                                value="<%= product ? product.name : '' %>"
                                required
                            >
                            <div class="invalid-feedback">
                                Nome é obrigatório
                            </div>
                        </div>

                        <div class="form-group mb-3">
                            <label for="reference" class="form-label">Referência</label>
                            <input 
                                type="text" 
                                class="form-control" 
                                id="reference" 
                                name="reference" 
                                value="<%= product ? product.reference : '' %>"
                            >
                        </div>

                        <div class="form-group mb-3">
                            <label for="barcode" class="form-label">Código de Barras</label>
                            <input 
                                type="text" 
                                class="form-control" 
                                id="barcode" 
                                name="barcode" 
                                value="<%= product ? product.barcode : '' %>"
                            >
                        </div>

                        <div class="form-group mb-3">
                            <label for="family_id" class="form-label">Família de Produtos</label>
                            <select class="form-control" id="family_id" name="family_id">
                                <option value="">Selecione uma família</option>
                                <% if (families && families.length > 0) { %>
                                    <% families.forEach(family => { %>
                                        <option 
                                            value="<%= family.id %>" 
                                            <%= product && product.family_id == family.id ? 'selected' : '' %>
                                        >
                                            <%= family.name %>
                                        </option>
                                    <% }); %>
                                <% } %>
                            </select>
                        </div>

                        <!-- Preços -->
                        <div class="form-group mb-3">
                            <label for="purchase_price" class="form-label">Preço de Compra (€)</label>
                            <input 
                                type="number" 
                                class="form-control" 
                                id="purchase_price" 
                                name="purchase_price" 
                                step="0.01" 
                                min="0"
                                value="<%= product ? product.purchase_price : '' %>"
                            >
                        </div>

                        <div class="form-group mb-3">
                            <label for="sale_price" class="form-label">Preço de Venda (€)</label>
                            <input 
                                type="number" 
                                class="form-control" 
                                id="sale_price" 
                                name="sale_price" 
                                step="0.01" 
                                min="0"
                                value="<%= product ? product.sale_price : '' %>"
                            >
                        </div>
                    </div>

                    <!-- Coluna Direita -->
                    <div class="col-md-6">
                        <!-- Estoque -->
                        <div class="form-group mb-3">
                            <label for="current_stock" class="form-label">Estoque Atual</label>
                            <input 
                                type="number" 
                                class="form-control" 
                                id="current_stock" 
                                name="current_stock" 
                                min="0"
                                value="<%= product ? product.current_stock : '0' %>"
                            >
                        </div>

                        <div class="form-group mb-3">
                            <label for="min_stock" class="form-label">Estoque Mínimo</label>
                            <input 
                                type="number" 
                                class="form-control" 
                                id="min_stock" 
                                name="min_stock" 
                                min="0"
                                value="<%= product ? product.min_stock : '0' %>"
                            >
                        </div>

                        <!-- Características Físicas -->
                        <div class="form-group mb-3">
                            <label for="weight" class="form-label">Peso</label>
                            <div class="input-group">
                                <input 
                                    type="number" 
                                    class="form-control" 
                                    id="weight" 
                                    name="weight" 
                                    step="0.001" 
                                    min="0"
                                    value="<%= product ? product.weight : '' %>"
                                >
                                <select class="form-select" name="weight_unit" style="max-width: 80px;">
                                    <option value="g" <%= product && product.weight_unit === 'g' ? 'selected' : '' %>>g</option>
                                    <option value="kg" <%= product && product.weight_unit === 'kg' ? 'selected' : '' %>>kg</option>
                                </select>
                            </div>
                        </div>

                        <div class="form-group mb-3">
                            <label for="dimensions" class="form-label">Dimensões</label>
                            <input 
                                type="text" 
                                class="form-control" 
                                id="dimensions" 
                                name="dimensions" 
                                placeholder="Ex: 10x5x2 cm"
                                value="<%= product ? product.dimensions : '' %>"
                            >
                        </div>

                        <!-- Materiais e Estilo -->
                        <div class="form-group mb-3">
                            <label for="material" class="form-label">Material</label>
                            <input 
                                type="text" 
                                class="form-control" 
                                id="material" 
                                name="material" 
                                value="<%= product ? product.material : 'Prata 925' %>"
                            >
                        </div>

                        <div class="form-group mb-3">
                            <label for="style" class="form-label">Estilo</label>
                            <input 
                                type="text" 
                                class="form-control" 
                                id="style" 
                                name="style" 
                                value="<%= product ? product.style : '' %>"
                            >
                        </div>
                    </div>
                </div>

                <!-- Descrição -->
                <div class="form-group mb-3">
                    <label for="description" class="form-label">Descrição</label>
                    <textarea 
                        class="form-control" 
                        id="description" 
                        name="description" 
                        rows="3"
                    ><%= product ? product.description : '' %></textarea>
                </div>

                <!-- Notas -->
                <div class="form-group mb-3">
                    <label for="notes" class="form-label">Notas Internas</label>
                    <textarea 
                        class="form-control" 
                        id="notes" 
                        name="notes" 
                        rows="2"
                    ><%= product ? product.notes : '' %></textarea>
                </div>

                <!-- Configurações -->
                <div class="row">
                    <div class="col-md-4">
                        <div class="form-check mb-3">
                            <input 
                                class="form-check-input" 
                                type="checkbox" 
                                id="is_active" 
                                name="is_active"
                                <%= !product || product.is_active ? 'checked' : '' %>
                            >
                            <label class="form-check-label" for="is_active">
                                Produto Ativo
                            </label>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="form-check mb-3">
                            <input 
                                class="form-check-input" 
                                type="checkbox" 
                                id="is_catalog_visible" 
                                name="is_catalog_visible"
                                <%= !product || product.is_catalog_visible ? 'checked' : '' %>
                            >
                            <label class="form-check-label" for="is_catalog_visible">
                                Visível no Catálogo
                            </label>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="form-check mb-3">
                            <input 
                                class="form-check-input" 
                                type="checkbox" 
                                id="featured" 
                                name="featured"
                                <%= product && product.featured ? 'checked' : '' %>
                            >
                            <label class="form-check-label" for="featured">
                                Produto em Destaque
                            </label>
                        </div>
                    </div>
                </div>

                <!-- Imagem -->
                <div class="form-group mb-4">
                    <label for="image" class="form-label">Imagem do Produto</label>
                    <input 
                        type="file" 
                        class="form-control" 
                        id="image" 
                        name="image" 
                        accept="image/*"
                    >
                    <% if (product && product.image_url) { %>
                        <div class="mt-2">
                            <img src="/media/products/<%= product.image_url %>" alt="<%= product.name %>" style="max-width: 150px; height: auto;">
                            <p class="text-muted mt-1">Imagem atual: <%= product.image_url %></p>
                        </div>
                    <% } %>
                </div>

                <!-- Botões -->
                <div class="form-group d-flex justify-content-between">
                    <a href="/admin/products" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i> Voltar
                    </a>
                    <div>
                        <% if (product && product.id) { %>
                            <button 
                                type="button" 
                                class="btn btn-danger me-2"
                                data-notification="Produto excluído com sucesso!"
                                data-notification-type="success"
                                onclick="confirmDelete(<%= product.id %>)"
                            >
                                <i class="fas fa-trash"></i> Excluir
                            </button>
                        <% } %>
                        <button 
                            type="submit" 
                            class="btn btn-primary"
                            data-notification="Produto salvo com sucesso!"
                            data-notification-type="success"
                        >
                            <i class="fas fa-save"></i> Salvar
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

<% if (product && product.id) { %>
<script>
function confirmDelete(productId) {
    if (confirm('Tem certeza que deseja excluir este produto? Esta ação não pode ser desfeita.')) {
        // Mostrar notificação de carregamento
        notifications.warning('Excluindo produto...', 2000);
        
        // Criar formulário para DELETE
        const form = document.createElement('form');
        form.method = 'POST';
        form.action = `/admin/products/${productId}`;
        
        const methodInput = document.createElement('input');
        methodInput.type = 'hidden';
        methodInput.name = '_method';
        methodInput.value = 'DELETE';
        
        form.appendChild(methodInput);
        document.body.appendChild(form);
        form.submit();
    }
}

// Validação de formulário
(function() {
    'use strict';
    window.addEventListener('load', function() {
        const forms = document.getElementsByClassName('needs-validation');
        Array.prototype.filter.call(forms, function(form) {
            form.addEventListener('submit', function(event) {
                if (form.checkValidity() === false) {
                    event.preventDefault();
                    event.stopPropagation();
                    notifications.error('Por favor, corrija os erros no formulário antes de continuar.', 5000);
                }
                form.classList.add('was-validated');
            }, false);
        });
    }, false);
})();
</script>
<% } %>
