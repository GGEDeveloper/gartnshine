<div class="container-fluid">
  <div class="row">
    <div class="col-12">
      <div class="page-title-box">
        <h4 class="page-title"><%= title %></h4>
        <div class="page-title-right">
          <a href="/admin/products/add" class="btn btn-primary">
            <i class="fas fa-plus me-1"></i> Adicionar Produto
          </a>
        </div>
      </div>
    </div>
  </div>

  <div class="card mb-3">
    <div class="card-header">
      <h5 class="card-title mb-0">Filtros</h5>
    </div>
    <div class="card-body">
      <form id="product-filters" class="row g-2" method="GET" action="/admin/products">
        <div class="col-md-3">
          <input type="text" id="filter-reference" name="reference" class="form-control form-control-sm" placeholder="Referência" value="<%= typeof queryParams !== 'undefined' && queryParams.reference ? queryParams.reference : '' %>">
        </div>
        <div class="col-md-3">
          <select id="filter-category" name="category" class="form-select form-select-sm">
            <option value="">Todas as Categorias</option>
            <% if (typeof productFamilies !== 'undefined' && productFamilies.length > 0) { %>
              <% productFamilies.forEach(family => { %>
                <option value="<%= family.name %>" <%= typeof queryParams !== 'undefined' && queryParams.category === family.name ? 'selected' : '' %>><%= family.name %></option>
              <% }); %>
            <% } %>
          </select>
        </div>
        <div class="col-md-2">
          <select id="filter-status" name="status" class="form-select form-select-sm">
            <option value="">Todos os Status</option>
            <option value="Ativo" <%= typeof queryParams !== 'undefined' && queryParams.status === 'Ativo' ? 'selected' : '' %>>Ativo</option>
            <option value="Inativo" <%= typeof queryParams !== 'undefined' && queryParams.status === 'Inativo' ? 'selected' : '' %>>Inativo</option>
          </select>
        </div>
        <div class="col-md-2">
          <select id="filter-stock" name="stock_status" class="form-select form-select-sm">
            <option value="">Todos</option>
            <option value="in_stock" <%= typeof queryParams !== 'undefined' && queryParams.stock_status === 'in_stock' ? 'selected' : '' %>>Em Stock</option>
            <option value="low_stock" <%= typeof queryParams !== 'undefined' && queryParams.stock_status === 'low_stock' ? 'selected' : '' %>>Stock Baixo</option>
            <option value="out_of_stock" <%= typeof queryParams !== 'undefined' && queryParams.stock_status === 'out_of_stock' ? 'selected' : '' %>>Fora de Stock</option>
          </select>
        </div>
        <div class="col-md-1">
          <button type="submit" class="btn btn-primary btn-sm w-100">Filtrar</button>
        </div>
        <div class="col-md-1">
          <a href="/admin/products" id="reset-filters" class="btn btn-outline-secondary btn-sm w-100">Limpar</a>
        </div>
      </form>
    </div>
  </div>

  <div class="row">
    <div class="col-12">
      <div class="card">
        <div class="card-body">
          <% if (products && products.length > 0) { %>
            <div class="table-responsive">
              <table class="table table-hover mb-0 datatable" style="width:100%">
                <thead>
                  <tr>
                    <th>ID</th>
                    <th>Imagem</th>
                    <th>Referência</th>
<th>Categoria</th>
<th>Nome</th>
                    <th>Preço</th>
                    <th>Stock</th>
                    <th>Status</th>
                    <th class="text-end">Ações</th>
                  </tr>
                </thead>
                <tbody>
                  <% products.forEach(product => { %>
                    <tr 
                      data-family-name="<%= product.family_name || '' %>" 
                      data-is-active="<%= product.is_active %>" 
                      data-stock-quantity="<%= product.current_stock || 0 %>"
                      data-reference="<%= product.reference || '' %>"
                    >
                      <td><%= product.id %></td>
                      <td>
                        <% if (product.image_url) { %>
                          <img src="/media/products/<%= product.image_url %>" alt="<%= product.name %>" class="img-thumbnail" style="width: 50px; height: 50px; object-fit: cover;">
                        <% } else { %>
                          <div class="bg-light d-flex align-items-center justify-content-center" style="width: 50px; height: 50px;">
                            <i class="fas fa-box text-muted"></i>
                          </div>
                        <% } %>
                      </td>
                      <td><%= product.reference || 'N/A' %></td>
<td><%= product.family_name || '' %></td>
<td><%= product.name %></td>
                      <td><%= parseFloat(product.price || 0).toLocaleString('pt-PT', {style: 'currency', currency: 'EUR'}) %></td>
                      <td>
                        <span class="badge bg-<%= product.current_stock > 10 ? 'success' : product.current_stock > 0 ? 'warning' : 'danger' %>">
                          <%= product.current_stock || 0 %>
                        </span>
                      </td>
                      <td>
                        <span class="badge bg-<%= product.is_active ? 'success' : 'secondary' %>">
                          <%= product.is_active ? 'Ativo' : 'Inativo' %>
                        </span>
                      </td>
                      <td class="text-end">
                        <div class="btn-group">
                          <a href="/admin/products/edit/<%= product.id %>" class="btn btn-sm btn-outline-primary">
                            <i class="fas fa-edit"></i>
                          </a>
                          <a href="/admin/products/delete/<%= product.id %>" 
                             class="btn btn-sm btn-outline-danger"
                             onclick="return confirm('Tem certeza que deseja excluir este produto?')">
                            <i class="fas fa-trash"></i>
                          </a>
                        </div>
                      </td>
                    </tr>
                  <% }); %>
                </tbody>
              </table>
            </div>

            <% if (typeof totalPages !== 'undefined' && totalPages > 1) { %>
              <nav class="mt-3">
                <ul class="pagination justify-content-center">
                  <% if (currentPage > 1) { %>
                    <li class="page-item">
                      <a class="page-link" href="?page=<%= currentPage - 1 %>&limit=<%= limit %>">Anterior</a>
                    </li>
                  <% } %>
                  
                  <% for (let i = 1; i <= totalPages; i++) { %>
                    <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                      <a class="page-link" href="?page=<%= i %>&limit=<%= limit %>"><%= i %></a>
                    </li>
                  <% } %>
                  
                  <% if (currentPage < totalPages) { %>
                    <li class="page-item">
                      <a class="page-link" href="?page=<%= currentPage + 1 %>&limit=<%= limit %>">Próximo</a>
                    </li>
                  <% } %>
                </ul>
              </nav>
            <% } %>
          <% } else { %>
            <div class="alert alert-info mb-0">
              Nenhum produto encontrado. <a href="/admin/products/add">Adicionar um novo produto</a>.
            </div>
          <% } %>
        </div>
      </div>
    </div>
  </div>

</div>

<% locals.styles = locals.styles || []; %>
<% locals.styles.push(`
<style>
.filter-row {
  display: flex;
  flex-wrap: wrap;
  gap: 0.75rem; /* Space between filter groups */
  align-items: flex-end; /* Align items to the bottom */
}

.filter-group {
  flex: 1; /* Allow groups to grow */
  min-width: 150px; /* Minimum width for each filter group */
  /* margin-bottom: 0.5rem; /* Add some bottom margin if flex-wrap causes multiple lines */
}

.filter-group.compact { 
  flex: 0 1 120px; /* Don't grow, basis of 120px */
  max-width: 120px;
}

.filter-group.actions {
  flex: 0 0 auto; /* Don't grow or shrink */
  display: flex;
  gap: 0.5rem; /* Space between action buttons */
  /* margin-left: auto; /* Pushes actions to the right if using a fixed-width container */
}

.filter-group label {
  display: block;
  margin-bottom: 0.25rem;
  font-size: 0.8rem;
  font-weight: 500;
  white-space: nowrap; /* Prevent labels from wrapping */
}
</style>
`); %>

<% locals.scripts = locals.scripts || []; %>
<% locals.scripts.push('/js/admin/product-filters.js'); // Placeholder if we move to external file %>
<!-- 
<script>
console.log('Inline script block started.');
if (typeof jQuery === 'undefined') {
  console.error('jQuery is NOT LOADED before this script block!');
}
$(document).ready(function() {
  console.log('Document ready. Initializing DataTables...');

  var table = $('.datatable').DataTable({
    // Optional: Add language settings if you have the pt-BR.json loaded
    // language: {
    //   url: '//cdn.datatables.net/plug-ins/1.13.4/i18n/pt-BR.json'
    // }
  });

  if (table && table.settings) {
    console.log('DataTables initialized successfully.');
  } else {
    console.error('DataTables FAILED to initialize.');
    return; // Stop if DataTables didn't initialize
  }

  // Prevent form submission on Enter key for text filter inputs
  $('#product-filters input[type="text"]').on('keypress', function(e) {
    if (e.which === 13) { // 13 is the Enter key
      console.log('Enter key pressed in text input. Preventing default form submission.');
      e.preventDefault();
      // Trigger keyup to make sure DataTables search is applied
      $(this).trigger('keyup');
    }
  });

  // Referência
  $('#filter-reference').on('keyup change', function() {
    var searchValue = this.value;
    console.log('Filter Reference: KeyUp/Change. Value: ' + searchValue + ' Column: 1');
    table.column(1).search(searchValue, false, false).draw(); // regex=false, smartSearch=false
    console.log('Reference filter applied. Rows before draw:', table.rows({ search: 'applied' }).nodes().length);
    console.log('Reference filter drawn. Visible rows:', table.rows({ page: 'current' }).nodes().length);
  });

  // Categoria
  $('#filter-category').on('change', function() {
    var searchValue = this.value;
    console.log('Filter Category: Change. Value:', searchValue, 'Column: 3');
    table.column(3).search(searchValue, false, false); // regex=false, smartSearch=false
    console.log('Category filter applied. Rows before draw:', table.rows({ search: 'applied' }).nodes().length);
    table.draw();
    console.log('Category filter drawn. Visible rows:', table.rows({ page: 'current' }).nodes().length);
  });

  // Status
  $('#filter-status').on('change', function() {
    var searchValue = this.value;
    console.log('Filter Status: Change. Value:', searchValue, 'Column: 5');
    table.column(5).search(searchValue, false, false); // regex=false, smartSearch=false
    console.log('Status filter applied. Rows before draw:', table.rows({ search: 'applied' }).nodes().length);
    table.draw();
    console.log('Status filter drawn. Visible rows:', table.rows({ page: 'current' }).nodes().length);
  });

  // Stock
  $('#filter-stock').on('change', function() {
    var searchValue = this.value;
    console.log('Filter Stock: Change. Value:', searchValue, 'Column: 6');
    
    // Clear any previous search on the stock column first
    table.column(6).search('', true, false); // Clear previous regex search, then draw immediately to reflect this clear

    if (searchValue === 'in_stock') {
      console.log('Stock: Searching for in_stock (^[1-9][0-9]*$)');
      table.column(6).search('^[1-9][0-9]*$', true, false); // isRegex=true, smartSearch=false
    } else if (searchValue === 'low_stock') {
      console.log('Stock: Searching for low_stock (^(?:[1-9]|10)$)');
      table.column(6).search('^(?:[1-9]|10)$', true, false); // isRegex=true, smartSearch=false
    } else if (searchValue === 'out_of_stock') {
      console.log('Stock: Searching for out_of_stock (^0$)');
      table.column(6).search('^0$', true, false); // isRegex=true, smartSearch=false
    } else {
      console.log('Stock: Clearing filter (selected "Todos" or empty)');
      // table.column(6).search('', true, false); // Already cleared above
    }
    // It's important to draw after setting the search term or after clearing it if no specific stock status is selected.
    console.log('Stock filter applied. Rows before draw:', table.rows({ search: 'applied' }).nodes().length);
    table.draw();
    console.log('Stock filter drawn. Visible rows:', table.rows({ page: 'current' }).nodes().length);
  });

  // Limpar filtros
  $('#reset-filters').on('click', function() {
    console.log('Reset Filters button clicked.');
    $('#product-filters')[0].reset(); // Reset form fields

    console.log('Triggering change/keyup on individual filters for reset...');
    // These triggers will cause their respective search functions to run with empty values,
    // which should clear their specific column filters and redraw the table.
    $('#filter-reference').trigger('keyup'); 
    $('#filter-category').trigger('change'); 
    $('#filter-status').trigger('change');   
    $('#filter-stock').trigger('change');    

    console.log('Filters reset.');
  });
});
</script>
-->
