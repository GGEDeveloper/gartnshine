<style>
  /* Base alert styles */
  .alert-container {
    z-index: 1000;
  }

  /* Alert box */
  .alert {
    padding: 1rem;
    margin-bottom: 1rem;
    border-radius: 0.5rem;
    box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
    display: flex;
    align-items: flex-start;
  }

  /* Alert types */
  .success-alert {
    background-color: #f0fdf4;
    border: 1px solid #bbf7d0;
    color: #166534;
  }

  .error-alert {
    background-color: #fef2f2;
    border: 1px solid #fecaca;
    color: #991b1b;
  }

  .warning-alert {
    background-color: #fffbeb;
    border: 1px solid #fef08a;
    color: #92400e;
  }

  .info-alert {
    background-color: #eff6ff;
    border: 1px solid #bfdbfe;
    color: #1e40af;
  }

  /* Alert icons */
  .alert i.fa-circle-check {
    color: #22c55e;
  }

  .alert i.fa-circle-xmark {
    color: #ef4444;
  }

  .alert i.fa-triangle-exclamation {
    color: #eab308;
  }

  .alert i.fa-circle-info {
    color: #3b82f6;
  }

  /* Alert close button */
  .alert button {
    opacity: 0.7;
    transition: opacity 150ms ease-in-out;
  }
  
  .alert button:hover {
    opacity: 1;
  }

  /* Responsive adjustments */
  @media (max-width: 640px) {
    .alert-container {
      left: 1rem;
      right: 1rem;
      width: auto;
    }
  }
</style>
