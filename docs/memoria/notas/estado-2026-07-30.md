---
slug: estado-2026-07-30
tipo: estado
dominio: geral
titulo: Estado a 2026-07-31 (fechado) — marca Gonzaga, homepage publicada, token do Instagram expirado
resumo: "Estado do trabalho em 2026-07-30 — homepage-redesign já publicada, estado local-only da BD e itens de segurança adiados"
valid_from: 2026-07-30
valid_to: 2026-08-17
ingested_at: 2026-08-17T14:13:37+00:00
superseded_by: trabalho-em-curso-2026-08
confianca: 1.0
entities:
  - gonzagas_node/config/brand.js
  - docs/rebranding/PLANO.md
  - og-artnshine.jpg
  - docs/DEPLOY_CATEGORIAS_GALERIA.md
  - scripts/backfill-family-slugs.js
  - gonzagas_node/README.md
  - CREDENCIAIS_ADMIN.md
  - com.js
  - server.js
  - app.js
  - waphix
sources:
  - migracao:project_estado_2026_07_30.md
---

Estado em **2026-07-31** do trabalho em artnshine.pt (`gonzagas_node/`):

- **A marca mudou para "Gonzaga"** (SEO e schema.org: "Gonzaga Jewellery").
  Vive toda em `gonzagas_node/config/brand.js`, exposta às vistas em
  `app.locals.brand` — **nenhum ficheiro fora daí escreve o nome**. Mudá-lo
  outra vez é mudar um ficheiro. Checkpoint anterior na tag
  `antes-rebranding-gonzaga`.
- O nome antigo fica **de propósito** no `alternateName` do schema.org, para
  o Google ligar as duas identidades. É a única ocorrência no HTML servido, 1
  por página. Retirar só daqui a ~6 meses — ver `docs/rebranding/PLANO.md`.
- Ainda com a marca antiga e **por fazer** (é design): `public/logo.svg`, a
  imagem de partilha `og-artnshine.jpg` e os ícones.

- A branch `homepage-redesign` **já foi integrada em `main` e publicada**
  (`ff44d57`), e o deploy correu: produção confirmada com o sitemap a apontar
  para `/media/`, categorias vazias excluídas e as meta descriptions novas.
- O trabalho seguinte (separação categoria/coleção/galeria, `/categoria/:slug`
  e `/galeria`) está em `docs/DEPLOY_CATEGORIAS_GALERIA.md` — ver
  [[conceitos-categoria-colecao-galeria]]. Exige o
  `scripts/backfill-family-slugs.js` em produção, senão as categorias ficam
  inacessíveis.
- Instagram: o token do `.env` **expirou a 10/07/2026** e não pode ser
  renovado — é preciso gerar um novo e colá-lo em `/admin/instagram`. A partir
  daí renova-se sozinho. *Stories não são suportadas* pela API Instagram Login
  e mesmo pela Graph API expiram em 24h; ficam posts, reels e carrosséis.
- **Não há forma de obter os posts sem token** — testado a 2026-07-31: a
  página pública do perfil devolve `post_shortcode_to_uri_mapping:{}` vazio,
  `?__a=1` dá 201 sem corpo, o oembed público exige token desde 2020 e o
  `/feed/` já não é RSS. Só o embed de um post individual
  (`/p/{shortcode}/embed/`) funciona, mas não há como listar os shortcodes.
  Sem dados, o site não mostra nada partido: as secções não são desenhadas.
- **A BD local tem estado que não deve ir para produção:** 9 registos de teste
  em `instagram_media`, 23 produtos com
  `featured=1` e duas coleções de teste ("Pedras Naturais", "Serpentes") com
  produtos associados. Foram criados para testar as secções da página inicial.
  Em produção as coleções curadas começam vazias — é o esperado, não uma falha
  de deploy.
- **Adiado pelo utilizador para o fim de tudo** ("fazemos isso agora no fim de
  tudo"): as chaves Stripe guardadas em texto simples na base de dados e a
  limpeza de credenciais do histórico do git. Não abrir estes temas antes de o
  utilizador os retomar.
- Ainda por tratar, encontrado de passagem: `gonzagas_node/README.md` e
  `CREDENCIAIS_ADMIN.md` têm passwords de admin em texto simples, e
  `docs/client_secret_*.apps.googleusercontent.com.json` é um segredo OAuth do
  Google (está **untracked**, nunca entrou no git). Pertencem ao mesmo lote de
  segurança adiado.
- O servidor local corre com `node server.js` (não `app.js` — esse não faz
  `listen`, está comentado). Em background precisa de sandbox desactivada,
  senão o `listen` é bloqueado.

Ver [[seo-audit-2026-07-30]], [[waphix-production-infra]] e
[[db-dev-vs-production]].
